{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Edge",
            "request": "launch",
            "type": "blazorwasm",
            "browser": "edge",
            "webRoot": "${workspaceFolder}"
        }
    ]
}